# --- Author's private material and full draft history (NOT for publication) ---
# Contains pre-public drafts, superseded companion docs, internal notes, and the
# editorial review. Kept locally for reference; never published.
archive/

# Sibling repos (ducp-node-rs, org-profile) live next to this checkout under the
# workspace parent — see WORKSPACE.md. Do not nest them inside the spec repo.

# --- LibreOffice / Microsoft Office lock & temporary files ---
.~lock.*#
~$*
*.tmp

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*~
*.bak
